---
description: Check and fix formatting
agent: build
---

Run `npm run format` to check Prettier formatting.
If there are formatting issues, run `npm run format:fix` to auto-fix them.
Report which files were reformatted.
