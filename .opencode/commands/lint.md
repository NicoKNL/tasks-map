---
description: Run ESLint and fix issues
agent: build
---

Run `npm run lint` to check for ESLint issues in `src/`.
If there are errors, fix them manually (do not use `--fix` blindly).
Follow the project conventions in AGENTS.md. Re-run lint to verify all issues are resolved.
