---
description: Run tests and fix failures
agent: build
---

Run the test suite with `npm test`.
Analyze any failures and fix them. After fixing, re-run the tests to verify.
If all tests pass, report a summary of what was tested.
